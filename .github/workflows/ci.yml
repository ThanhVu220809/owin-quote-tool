name: CI

on:
  push:
    branches: [main, full-reference-parity]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: owin-quote-tool
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: owin-quote-tool/package-lock.json
      - run: npm ci
      # Lint hiện còn nhiều cảnh báo tồn đọng → báo lỗi nhưng không chặn build.
      - run: npm run lint
        continue-on-error: true
      - run: npm test
      - run: npm run build
